export type FindOptions<TEntity> = {
  where?: Partial<TEntity>;
};

export type FindAndCountOptions<TEntity> = {
  where?: Partial<TEntity>;
};

export type FindOneOptions<TEntity> = {
  where?: Partial<TEntity>;
};
